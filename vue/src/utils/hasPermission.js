import store from '../store'

export function hasPermission(permission) {
  return true;
}

export function hasPermissionNotButtom(permission) {
  return true;
  // return hasPermission(permission);
}
